# AI Agent Chat Contract Test

Manual/agent checklist until real test runner is wired.

- app manifest exists
- every block manifest exists
- every block ID starts with `ai-agent-chat.`
- every block has version `0.1.0`
- every block has empty permissions by default
- messages exist for EN/RU
- hotkeys are declared outside UI components
- icons are declared outside UI components
- no Core files are required by this skeleton
