# Gate Notes

This template intentionally uses no native permissions.
A real Fractal App must add only the exact permissions needed by its blocks.

Fast mode:

```txt
run targeted gate first
run full validation once at block end
never run cargo clean unless required by cache/Rust error
```
