# Block UI

Put block-specific UI here only when this block owns the UI.

Rules:

```txt
Use src/shared/ui components.
Use shared design tokens.
Use UiIcon.
Use message keys.
No hardcoded colors/z-index/font sizes.
```
