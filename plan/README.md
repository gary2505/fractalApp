# Plan

Feature plans live here.

```txt
plan = what to build + how to test
.ai  = reusable agent rules
src  = app code
```

Before coding a feature, use its plan.

If plan is missing, create short plan first.
