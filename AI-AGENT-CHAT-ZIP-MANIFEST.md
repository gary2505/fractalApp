# ZIP Manifest: fractalApp AI Agent Chat Skeleton

This ZIP contains only new files for the `fractal-app-ai-agent-chat` skeleton.

It does not change:

- `package.json`
- `src/core/**`
- existing shared foundation files
- real Fractal Apps
- provider integrations

Install rule:

If any file already exists, stop and report conflict before overwrite.
