# block-agent-skills

Purpose:
Small skills for AI agents.

Rule:
Router -> exact skills -> exact files -> small patch -> smallest check.

Added:
- Core-Web boundary skills
- CoreBridge/Policy skill
- manifest/update skill
- fractal vertical slice skill
- next-session agent memory

Update v0.3:
- added system skills
- agent must use `src/system` for async/input/runtime/diag
- system never imports feature/UI
