# All button IDs list

**`top-row`** (top-row-v1-0.svelte):
| Button | ID |
| Tab buttons (each loop) | `top-row-tab-{tab.id}` |
| Drag window | `top-row-drag` |
| Terminal toggle | `top-row-terminal-toggle` |
| Switch agent side | `top-row-switch-side` |
| Minimize | `top-row-minimize` |
| Maximize | `top-row-maximize` |
| Close | `top-row-close` |

**`top-bar`** (top-bar-v1-0.svelte):
| Button | ID |
| AI chat toggle (sparkle, both sides) | `top-bar-ai-toggle` |
| Left panel toggle (both sides) | `top-bar-panel-toggle` |

**`status-bar`** (status-bar-v1-0.svelte):
| Button | ID |
| Lang EN | `status-bar-lang-en` |
| Lang RU | `status-bar-lang-ru` |
| Theme select | `status-bar-theme` |

**`p0`** (p0-v1-0.svelte):
| Button | ID |
| Rail items (each loop) | `p0-rail-{item.id}` |

**`p3h1`** (p3h1-v1-0.svelte):
| Button | ID |
| Tab buttons (each loop) | `p3h1-tab-{tab.toLowerCase()}` |
| More tabs | `p3h1-tab-more` |

**`p3h2`** (p3h2-v1-0.svelte):
| Button | ID |
| Tab buttons (each loop) | `p3h2-tab-{tab.toLowerCase()}` |
| Pin toggle | `p3h2-pin-toggle` |
| Maximize toggle | `p3h2-maximize-toggle` |
| Close | `p3h2-close` |

**`p4`** (p4-v1-0.svelte):
| Button | ID |
| Tab buttons (each loop) | `p4-tab-{tab.toLowerCase()}` |
| Sidebar toggle | `p4-sidebar-toggle` |
| Close | `p4-close` |
| p4v2 Plan | `p4v2-plan` |
| p4v2 Diff | `p4v2-diff` |
| p4v2 Proof | `p4v2-proof` |
| p4v2 Logs | `p4v2-logs` |
