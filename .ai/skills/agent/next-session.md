# Next Session

Project: fractalApp.

Core idea:

```txt
stable Core + versioned Web Components + Fractal blocks
```

Rules:

```txt
read .ai/start.md first
read latest PROJECT-STAGE / PROJECT-STATE
use plan/* for feature work
use skills only as selected by plan/router
one task = one patch
short names only
Core must boot alone
Web Components never call Tauri/Rust directly
```

Current next target:

```txt
Settings Popup v1
Use plan/settings-popup.md
```
