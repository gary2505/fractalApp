# Popup

Required:
- focus moves inside on open
- Escape closes unless blocked
- focus returns to opener
- outside click rule is explicit
- close button has label
- no hardcoded user text

Use:
- shared popup/modal component
- shared z-index token
- shared motion token

Do not build popup CSS locally.
